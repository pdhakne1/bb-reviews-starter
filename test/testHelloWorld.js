var assert = require('assert');
var expect  = require('chai').expect;

it('Dummy test for integration', function() {
    assert.ok(true);
});

it('Other test for pipeline', function() {
    //test for okay.
    assert.ok(true);
});
